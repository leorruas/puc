---
title: "04. Sub-rotinas (funções e procedimentos)"
disciplina: "00. Sintaxe Multilinguagem"
modulo: "Sintaxe Multilinguagem"
tags:
  - sub-rotinas
  - funcoes
  - procedimentos
  - abstracao
  - csharp
  - java
  - python
  - javascript
relacionados:
  - "[[00. Sintaxe Multilinguagem/00. Guia multilinguagem de sintaxe e praticas - Índice]]"
---

# 04. Sub-rotinas (funções e procedimentos)

> **Contexto:** Estudo prático da diferença sintática entre Funções (abstração de expressão com retorno) e Procedimentos (abstração de comando `void`) em C#, Java, Python e JavaScript.

---

## 1. Como funciona

* **Função (Abstração de Expressão):** Recebe dados de entrada, calcula e **retorna obrigatoriamente um valor**. É usada em atribuições (`x = calcular()`).
* **Procedimento (Abstração de Comandos):** Executa uma sequência de instruções de ação (E/S, telas, gravações) e **não retorna valor** (`void` em C#/Java).

---

## 2. Comparativo multilinguagem

### C#
```csharp
// Procedimento (void)
public static void ExibirMensagem(string texto) {
    Console.WriteLine(texto);
}

// Função (retorna int)
public static int Somar(int a, int b) {
    return a + b;
}
```

### Java
```java
// Procedimento
public static void exibirMensagem(String texto) {
    System.out.println(texto);
}

// Função
public static int somar(int a, int b) {
    return a + b;
}
```

### Python
```python
# Procedimento (retorna None implicitamente)
def exibir_mensagem(texto):
    print(texto)

# Função (retorna valor explicitamente)
def somar(a, b):
    return a + b
```

### JavaScript
```javascript
// Procedimento
function exibirMensagem(texto) {
    console.log(texto);
}

// Função
function somar(a, b) {
    return a + b;
}
```

---

## Navegação

* **Tópico anterior:** [[00. Sintaxe Multilinguagem/03. Estruturas de repetição (while, for, do-while)|03. Estruturas de Repetição (while, for, do-while)]]
* **Próximo tópico:** [[00. Sintaxe Multilinguagem/05. Abstração e encapsulamento com classes (TADs)|05. Abstração e Encapsulamento com Classes (TADs)]]
* **Voltar ao Índice:** [[00. Sintaxe Multilinguagem/00. Guia multilinguagem de sintaxe e praticas - Índice]]
