---
title: "Tipos abstratos de dados"
disciplina: "01. Programacao Modular"
modulo: "Módulo 1: Introdução e conceitos iniciais"
tags:
  - tipos-abstratos-de-dados
  - tad
  - encapsulamento
  - orientacao-a-objetos
  - feynman
  - csharp
  - getters-e-setters
  - estruturas-de-dados
  - modelos-mentais
  - programacao-modular
relacionados:
  - "[[02. Funções e procedimentos]]"
  - "[[04. Programação orientada a objetos e acoplamento]]"
  - "[[00. Programação modular - Resumo]]"
  - "[[00. Sintaxe Multilinguagem/05. Abstração e encapsulamento com classes (TADs)]]"
  - "[[00. Sintaxe Multilinguagem/06. Modificadores de acesso, escopo e a palavra-chave static]]"
  - "[[00. Sintaxe Multilinguagem/07. Getters, setters e propriedades (acesso e modificação de estado)]]"
  - "[[00. Sintaxe Multilinguagem/08. Estruturas de dados fundamentais (materialização de modelos mentais em código)]]"
---

# Tipos abstratos de dados: a união de valores, operações e encapsulamento

> **Contexto:** Estudo formal dos Tipos Abstratos de Dados (TADs), abordando a definição de tipos de dados segundo a teoria dos conjuntos, a Orientação a Objetos como principal paradigma de implementação (Classes e Objetos), a regra de ouro do encapsulamento (atributos privados e operações públicas), a analogia do Caixa Eletrônico, a análise detalhada da classe Conta Bancária em C# e as estruturas de dados como modelos conceituais e estruturas de pensamento.

---

## Introdução e definição de tipo de dados

Para compreender o conceito de um **Tipo Abstrato de Dados (TAD)**, é preciso primeiro definir o que é um **tipo de dado** na ciência da computação.

Um tipo de dado representa a união formal entre dois componentes:
1. **Um conjunto de valores** possíveis.
2. **Um conjunto de operações** bem definidas que podem ser aplicadas sobre esses valores.

### Relação com a Teoria dos Conjuntos
Essa definição fundamenta-se na **teoria dos conjuntos da matemática**: todos os valores pertencentes ao mesmo tipo compartilham as mesmas propriedades algébricas e estão sujeitos às mesmas regras e formas de manipulação. 

Por exemplo, no tipo matemático/computacional `Inteiro`:
* **Conjunto de valores:** $\{\dots, -2, -1, 0, 1, 2, \dots\}$
* **Operações permitidas:** $\{+, -, \times, \div, \bmod\}$

---

## 1. O que é um Tipo Abstrato de Dados (TAD)?

Um **Tipo Abstrato de Dados (TAD)** é um módulo especial que **encapsula (ou esconde)** a forma como seus elementos e dados internos são implementados fisicamente na memória, mas disponibiliza uma interface com operações públicas para que esses elementos sejam manipulados de forma segura pelo mundo externo.

Nestes casos, o **módulo passa a ser um tipo abstrato de dados**, agregando:
* **Atributos (Dados/Estado - Privados `private`):** a representação interna das informações armazenadas.
* **Operações (Métodos/Funções/Procedimentos - Públicos `public`):** as ações permitidas que podem ser aplicadas sobre os dados armazenados nesses atributos.

```mermaid
graph LR
    subgraph MundoExterno ["Código Cliente (Mundo Externo)"]
        A["Operações Públicas<br>(Interface do TAD)"]
    end
    
    subgraph ModuloTAD ["Módulo (Tipo Abstrato de Dados)"]
        A -->|Invocação| B["Operações / Métodos Internos"]
        B --- C["Atributos Encapsulados<br>(Dados Escondidos)"]
    end
```

---

## 2. A intuição do Encapsulamento (Técnica de Feynman): O Caixa Eletrônico

Para entender por que **atributos são privados** e **operações são públicas**, pense em um **Caixa Eletrônico (ATM)**:

* **Atributos Privados (`private`):** O cofre de aço interno, as gavetas de dinheiro físico e os chips de contagem. Se o cofre fosse público, qualquer pessoa na rua poderia abrir a tampa com uma chave de fenda e alterar o saldo manualmente. O estado seria corrompido instantaneamente.
* **Operações Públicas (`public`):** A tela sensível ao toque, o teclado numérico e a abertura para retirada de cédulas (`Sacar()`, `Depositar()`, `ConsultarSaldo()`). Você só altera o dinheiro de dentro do cofre **passando obrigatoriamente pelas regras e validações da interface pública** (digitar a senha correta e verificar se há saldo suficiente).

---

## 3. A Orientação a Objetos como implementação de TADs

A **Programação Orientada a Objetos (POO)** é o principal paradigma de programação que implementa na prática o conceito de Tipos Abstratos de Dados:

> *"Neste paradigma, cada tipo representa uma **classe** de objetos, e cada objeto representa uma **instância** desta classe."*

* **Classe (O Tipo):** Funciona como o modelo/molde que define a estrutura dos dados (atributos) e quais operações (métodos) podem ser aplicadas $\rightarrow$ [[00. Sintaxe Multilinguagem/05. Abstração e encapsulamento com classes (TADs)|Veja o comparativo no Guia de Sintaxe]].
* **Objeto / Instância (O Valor do Conjunto):** Representa uma entidade concreta construída na memória a partir do modelo da classe, possuindo seu próprio estado independente.

---

## 4. Exemplo prático detalhado: Classe Conta Bancária em C#

Abaixo está a implementação completa da classe `Conta` em C#, seguida pelo detalhamento de cada uma das suas partes e papéis dentro do conceito de TAD:

```csharp
using System;
using System.Collections.Generic;

// 1. A CLASSE (MODELO DO TAD)
public class Conta 
{
  // 2. ATRIBUTOS PRIVADOS (ESTADO INTERNO ENCAPSULADO COM PREFIXO DE SUBLINHAR)
  private DateTime _criacao;
  private double _saldo = 0;

  // 3. CONSTRUTOR (INICIALIZAÇÃO CONTROLADA DA INSTÂNCIA)
  public Conta(double saldoInicial, DateTime criacao) 
  {
    // Copia o valor temporário recebido no parâmetro 'saldoInicial' para o atributo persistente '_saldo' da conta
    _saldo = saldoInicial;

    // Registra a data de criação: o parâmetro 'criacao' é um dado temporário que desaparece após a execução do construtor.
    // Atribuir '_criacao = criacao' é essencial para gravar permanentemente essa data no estado interno do objeto na memória.
    _criacao = criacao;
  }
  
  // 4. PROPRIEDADE DE LEITURA (CONSULTA DE ATRIBUTO IMUTÁVEL VIA GET)
  public DateTime DataCriacao 
  {
    get { return _criacao; }
  }

  // 5. OPERAÇÃO PÚBLICA DE SAQUE (FUNÇÃO QUE ALTERA ESTADO COM VALIDAÇÃO)
  public double Sacar(double quantia) {
    if (_saldo < quantia)
        throw new ArgumentException("Quantia de saque não permitida.", "quantia");
    _saldo -= quantia;
    return quantia;
  }

  // 6. OPERAÇÃO PÚBLICA DE DEPÓSITO (PROCEDIMENTO/MÉTODO VOID QUE ALTERA ESTADO)
  public void Depositar(double quantia) {
    if (quantia <= 0)
      throw new ArgumentException("Quantia de depósito não permitida.", "quantia");
    _saldo += quantia;
  }

  // 7. OPERAÇÃO DE CONSULTA DO ESTADO (MÉTODO GETTER)
  // Por que criamos 'GetSaldo()'? Como '_saldo' é private, o mundo externo não consegue ler contaDoZe._saldo diretamente.
  // Criamos essa função pública (um "Getter") para permitir que o cliente leia o valor atual com segurança (sem poder alterá-lo).
  public double GetSaldo() {
    return _saldo;
  }
}

// 8. CÓDIGO CLIENTE (CONSUMIDOR DO TAD)
public class MainClass {
  public static void Main (string[] args) {
    // Instanciação: cria a instância 'contaDoZe' do tipo 'Conta'
    Conta contaDoZe = new Conta(1200, DateTime.Now);

    // Invocação do Getter público: lê o saldo protegido
    Console.WriteLine ("Saldo da conta do Ze: {0:C2}", contaDoZe.GetSaldo());

    double quantia = 212;
    Console.WriteLine ("Sacar {0:C2} da conta do Ze.", quantia);
    contaDoZe.Sacar(quantia);

    Console.WriteLine ("Saldo da conta do Ze: {0:C2}", contaDoZe.GetSaldo());
  }
}
```

---

## 5. Detalhamento componente a componente do código

### a. Definição da classe (`public class Conta`)
Representa a **criação do Tipo Abstrato de Dados**. Ela especifica o contrato e a estrutura que todas as contas bancárias criadas pelo programa seguirão $\rightarrow$ [[00. Sintaxe Multilinguagem/06. Modificadores de acesso, escopo e a palavra-chave static#1. Desconstrução do cabeçalho clássico em C# (Técnica de Feynman)|Entenda o papel de class no Guia de Sintaxe]].

### b. Atributos privados (`private DateTime _criacao;` e `private double _saldo = 0;`)
* **Papel:** Representam o **estado interno** da conta.
* **Encapsulamento e Prefixo `_`:** O modificador `private` e o sublinhado `_` protegem e identificam atributos privados de classe $\rightarrow$ [[00. Sintaxe Multilinguagem/06. Modificadores de acesso, escopo e a palavra-chave static#2. Por que usar o sublinhado (_) no início de atributos (_saldo, _criacao)?|Entenda o uso do sublinhado (_) no Guia de Sintaxe]].

### c. Construtor (`public Conta(double saldoInicial, DateTime criacao)`)
* **Papel:** É o procedimento responsável pela **instanciação e inicialização**.
* **Por que `_criacao = criacao` é obrigatório?** Os parâmetros do construtor (`saldoInicial` e `criacao`) são **variáveis temporárias locais** que deixam de existir assim que o construtor termina. Para que a conta bancária continue lembrando quando foi criada durante toda a sua vida no sistema, o valor recebido em `criacao` precisa ser gravado e persistido no atributo interno `_criacao`.
* **Garantia de integridade:** Garante que nenhum objeto do tipo `Conta` seja criado na memória sem um saldo inicial e uma data de criação válidas.

### d. Propriedade de leitura (`public DateTime DataCriacao`)
* **Papel:** Expõe o acesso somente leitura (*read-only*) à data de criação através da palavra-chave `get` $\rightarrow$ [[00. Sintaxe Multilinguagem/07. Getters, setters e propriedades (acesso e modificação de estado)#3. As duas formas de implementar no C#: Métodos vs. Propriedades|Entenda a sintaxe de Propriedades no Guia de Sintaxe]].
* **Segurança:** Permite que o mundo externo consulte a data sem risco de modificá-la.

### e. Operação de saque (`public double Sacar(double quantia)`)
* **Papel:** É uma **função pública (abstração de expressão)** que realiza uma alteração de estado com validação $\rightarrow$ [[00. Sintaxe Multilinguagem/04. Sub-rotinas (funções e procedimentos)|Entenda a diferença de funções com retorno]].

### f. Operação de depósito (`public void Depositar(double quantia)`)
* **Papel:** É um **procedimento público (abstração de comandos - `void`)** que altera o estado interno $\rightarrow$ [[00. Sintaxe Multilinguagem/04. Sub-rotinas (funções e procedimentos)#2. Por que os procedimentos usam void em C#?|Entenda por que procedimentos usam void]].

### g. Operação de consulta (`public double GetSaldo()`)
* **Papel:** É uma **função de consulta de estado (Getter)** criada pelo desenvolvedor $\rightarrow$ [[00. Sintaxe Multilinguagem/07. Getters, setters e propriedades (acesso e modificação de estado)|Entenda Getters e Setters no Guia de Sintaxe]].
* **Por que `GetSaldo()` existe?** Como a variável `_saldo` é privada (`private`), o código externo (`MainClass`) seria impedido pelo compilador de fazer `contaDoZe._saldo`. A função `GetSaldo()` é a "janela segura" que expõe a leitura do valor sem dar permissão de escrita.

### h. O código cliente (`MainClass`)
* **Papel:** É o consumidor do TAD.
* **Desacoplamento:** O método `Main` interage com a instância `contaDoZe` **exclusivamente através da interface pública** (`Sacar`, `GetSaldo`), sem tocar nos atributos privados.

---

## 6. Por que essa separação (atributos privados vs. operações públicas) é tão importante?

A separação estrita entre o estado interno e a interface pública sustenta os três pilares fundamentais da engenharia de software modular $\rightarrow$ [[00. Sintaxe Multilinguagem/05. Abstração e encapsulamento com classes (TADs)#1. Como funciona (Técnica de Feynman)|Consulte o Guia de Sintaxe]]:

1. ==**Garantia de Integridade:**==
   ==Ninguém consegue forçar um valor absurdo ou corromper a memória (como `_saldo = -50000;`) porque o acesso direto à variável é bloqueado pelo compilador. Qualquer alteração passa obrigatoriamente pelos testes de validação dos métodos públicos.==

2. ==**Ocultamento da Informação (*Information Hiding*):**==
   ==O usuário ou código cliente que consome o módulo só precisa saber **o que** o módulo faz (a lista de métodos públicos da interface), sem precisar se preocupar com **como** os dados estão organizados por dentro na memória.==

3. ==**Liberdade para Alterar o Código Interno (Evolução sem quebras):**==
   ==Se no futuro o banco mudar a forma física de armazenamento do saldo (ex.: de uma variável simples `double` para uma lista de transações com taxas automáticas), a alteração é feita em **um único lugar** (dentro da classe `Conta`). Nenhum outro arquivo ou tela do sistema cliente sofrerá impacto ou precisará ser reescrito!==

---

## 7. Estruturas de dados como modelos mentais e estruturas de pensamento

==Na Ciência da Computação, **estruturas de dados não são apenas arquivos ou posições físicas de memória**. Elas representam, acima de tudo, **estruturas formais de pensamento e modelos conceituais** para organizar como a mente humana e os algoritmos resolvem problemas do mundo real:==

```mermaid
graph TD
    subgraph Abstracao ["Mundo Conceitual (Estruturas de Pensamento)"]
        M1["Modelo LIFO<br>(Desfazer / Histórico)"]
        M2["Modelo FIFO<br>(Ordem de Chegada / Atendimento)"]
        M3["Modelo Hierárquico<br>(Classificação / Árvore de Decisão)"]
        M4["Modelo em Rede<br>(Conexões / Grafos Sociais)"]
    end
    
    subgraph Concreto ["Implementação Física na Memória"]
        C1["Pilha (Stack)"]
        C2["Fila (Queue)"]
        C3["Árvore Binária (BST)"]
        C4["Grafo (Graph)"]
    end

    M1 -.->|Materializa-se em| C1
    M2 -.->|Materializa-se em| C2
    M3 -.->|Materializa-se em| C3
    M4 -.->|Materializa-se em| C4
```

* **Estruturas Ligadas a Processos Físicos e Cotidianos:** 
  - **Fila (FIFO):** O pensamento de justiça por ordem de chegada (quem chega primeiro é atendido primeiro).
  - **Pilha (LIFO):** O pensamento de sobreposição ou retrocesso (o último prato colocado na pilha é o primeiro que pegamos para lavar; a última ação feita no teclado é a primeira que desfazemos no `Ctrl+Z`).
* **Estruturas de Pensamento Lógico e Relacional:**
  - **Árvores:** O pensamento taxonômico e hierárquico (como classificar reinos biológicos, organogramas de empresas ou o sumário de um livro).
  - **Grafos:** O pensamento relacional em teia (como o cérebro conecta neurônios, estradas conectam cidades ou amizades se conectam em redes sociais).
  - **Tabelas Hash:** O pensamento de indexação direta por chave (como encontrar o significado de uma palavra em um dicionário usando a primeira letra em vez de ler o livro todo do início).

---

## 8. Catálogo completo das principais estruturas de dados e TADs

Abaixo está o panorama categorizado das estruturas de dados clássicas da Ciência da Computação (veja a sintaxe prática em [[00. Sintaxe Multilinguagem/08. Estruturas de dados fundamentais (materialização de modelos mentais em código)|Guia Prático de Estruturas de Dados]]):

### a. Estruturas Lineares (Sequenciais)

| Tipo Abstrato de Dados (TAD) | Atributos Internos (Encapsulados) | Operações Públicas (Interface) | Comportamento / Regra de Acesso | Guia de Sintaxe |
| --- | --- | --- | --- | --- |
| **Pilha (*Stack*)** | Array ou Lista encadeada + `topo` | `push(item)`, `pop()`, `top()`, `isEmpty()` | **LIFO** (*Last In, First Out*): o último que entra é o primeiro que sai. | [[00. Sintaxe Multilinguagem/08. Estruturas de dados fundamentais (materialização de modelos mentais em código)#2. Pilha (Stack - Modelo LIFO / Desfazer)\|Sintaxe]] |
| **Fila (*Queue*)** | Array circular ou Lista + `inicio`/`fim` | `enqueue(item)`, `dequeue()`, `front()` | **FIFO** (*First In, First Out*): o primeiro que entra é o primeiro a ser atendido. | [[00. Sintaxe Multilinguagem/08. Estruturas de dados fundamentais (materialização de modelos mentais em código)#3. Fila (Queue - Modelo FIFO / Ordem de Chegada)\|Sintaxe]] |
| **Lista (*List / Deque*)** | Array dinâmico ou nós encadeados (`head`/`tail`) | `insert()`, `remove()`, `get()`, `size()` | Acesso e inserção flexível em qualquer posição (início, meio ou fim). | [[00. Sintaxe Multilinguagem/08. Estruturas de dados fundamentais (materialização de modelos mentais em código)#4. Lista Dinâmica (List - Sequência Indexada)\|Sintaxe]] |
| **Vetor Dinâmico (*ArrayList*)** | Array estático interno com redimensionamento | `add()`, `get()`, `remove()`, `capacity()` | Acesso indexado instantâneo (`[i]`) com crescimento automático. | [[00. Sintaxe Multilinguagem/08. Estruturas de dados fundamentais (materialização de modelos mentais em código)#4. Lista Dinâmica (List - Sequência Indexada)\|Sintaxe]] |

### b. Estruturas de Associação (Chave-Valor) e Conjuntos

| Tipo Abstrato de Dados (TAD) | Atributos Internos (Encapsulados) | Operações Públicas (Interface) | Comportamento / Regra de Acesso | Guia de Sintaxe |
| --- | --- | --- | --- | --- |
| **Tabela Hash (*Dictionary / Map*)** | Array de baldes (*buckets*) + Função Hash | `put(chave, valor)`, `get(chave)`, `remove(chave)` | Busca e recuperação ultra-rápida baseada em chave única. | [[00. Sintaxe Multilinguagem/08. Estruturas de dados fundamentais (materialização de modelos mentais em código)#5. Tabela Hash / Dicionário (Dictionary / Map - Busca por Chave Única)\|Sintaxe]] |
| **Conjunto (*Set*)** | Tabela hash interna sem duplicatas | `add()`, `contains()`, `remove()`, `union()` | Coleção de elementos **únicos** (rejeita repetições) e sem ordem fixa. | [[00. Sintaxe Multilinguagem/08. Estruturas de dados fundamentais (materialização de modelos mentais em código)#6. Conjunto (Set - Unicidade sem Repetição)\|Sintaxe]] |

### c. Estruturas Não-Lineares (Hierárquicas e Redes)

| Tipo Abstrato de Dados (TAD) | Atributos Internos (Encapsulados) | Operações Públicas (Interface) | Comportamento / Regra de Acesso | Guia de Sintaxe |
| --- | --- | --- | --- | --- |
| **Árvore Binária (*BST*)** | Nós encadeados com referências `esq`/`dir` | `insert()`, `search()`, `remove()`, `inOrder()` | Estrutura em ramos ordenada para buscas binárias rápidas. | [[00. Sintaxe Multilinguagem/08. Estruturas de dados fundamentais (materialização de modelos mentais em código)\|Sintaxe]] |
| **Fila de Prioridade (*Heap*)** | Array representando uma árvore completa | `insert()`, `extractMax()`, `peek()` | O elemento de **maior prioridade/urgência** é sempre o primeiro a sair. | [[00. Sintaxe Multilinguagem/08. Estruturas de dados fundamentais (materialização de modelos mentais em código)\|Sintaxe]] |
| **Grafo (*Graph*)** | Matriz ou lista de adjacência (Vértices/Arestas) | `addVertex()`, `addEdge()`, `bfs()`, `dfs()` | Relacionamentos e conexões complexas em rede entre nós. | [[00. Sintaxe Multilinguagem/08. Estruturas de dados fundamentais (materialização de modelos mentais em código)\|Sintaxe]] |

---

## Referências bibliográficas

### Bibliografia básica
* LISKOV, Barbara; ZILLES, Stephen. **Programming with Abstract Data Types**. ACM SIGPLAN Notices, v. 9, n. 4, p. 50-59, April 1974. Disponível em: [https://dl.acm.org/doi/10.1145/942572.807045](https://dl.acm.org/doi/10.1145/942572.807045).
* WIRTH, Niklaus. **Algorithms + Data Structures = Programs**. Englewood Cliffs: Prentice-Hall, 1976.
* SOMMERVILLE, Ian. **Engenharia de Software**. 10. ed. São Paulo: Pearson, 2019.

### Bibliografia complementar
* TENENBAUM, Aaron M.; LANGSAM, Yedidyah; AUGENSTEIN, Moshe J. **Estruturas de Dados Usando C**. São Paulo: Makron Books, 1995.
* SEBESTA, Robert W. **Conceitos de Linguagens de Programação**. 11. ed. Porto Alegre: Bookman, 2018.

---

## Artigos relacionados e navegação

* **Artigo anterior:** [[02. Funções e procedimentos]]
* **Próximo artigo:** [[04. Programação orientada a objetos e acoplamento]]
* **Resumo da disciplina:** [[00. Programação modular - Resumo]]
* **Guia de Sintaxe Multilinguagem:** [[00. Sintaxe Multilinguagem/00. Guia multilinguagem de sintaxe e praticas - Índice]]
* **Índice geral do vault:** [[index.md|Página Inicial do Vault]]
