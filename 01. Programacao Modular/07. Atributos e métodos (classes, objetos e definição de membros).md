---
title: "Atributos e métodos (classes, objetos e definição de membros)"
disciplina: "01. Programacao Modular"
modulo: "Módulo 1: Introdução e conceitos iniciais"
tags:
  - classes-e-objetos
  - atributos
  - metodos
  - tipos-abstratos-de-dados
  - membros-de-classe
  - encapsulamento
  - feynman
  - csharp
  - java
  - programacao-modular
relacionados:
  - "[[06. Fatores internos de qualidade de software]]"
  - "[[04. Programação orientada a objetos e acoplamento]]"
  - "[[03. Tipos abstratos de dados]]"
  - "[[00. Programação modular - Resumo]]"
  - "[[00. Sintaxe Multilinguagem/05. Abstração e encapsulamento com classes (TADs)]]"
  - "[[00. Sintaxe Multilinguagem/06. Modificadores de acesso, escopo e a palavra-chave static]]"
  - "[[00. Sintaxe Multilinguagem/07. Getters, setters e propriedades (acesso e modificação de estado)]]"
---

# Atributos e métodos: a estrutura fundamental de classes e objetos

> **Contexto:** Na Programação Orientada a Objetos (POO), um Tipo Abstrato de Dados (TAD) é materializado na forma de uma **Classe**, que funciona como um molde estrutural agrupando **Objetos** que compartilham as mesmas propriedades. Este artigo detalha a definição técnica, a anatomia de **Atributos** (estado) e **Métodos** (comportamento), os modificadores de visibilidade e o ciclo de instanciação explicados pela Técnica de Feynman.

---

## 1. O que são classes, objetos, atributos e métodos? (Técnica de Feynman)

Imagine uma **fábrica de automóveis**:

```mermaid
flowchart TD
    subgraph Fabrica ["Da Planta Arquitetônica ao Produto Concreto"]
        direction TB
        Classe["1. A Classe (A Planta do Carro)<br>O desenho técnico no papel que define:<br>- Quais dados todo carro terá (chassi, cor, combustível).<br>- Quais ações todo carro fará (ligar, acelerar, frear)."]
        
        Objeto1["2. Objeto 1 (Instância na Garagem)<br>Carro Vermelho, Chassi 9BW..., 45L no tanque."]
        Objeto2["3. Objeto 2 (Outra Instância Independente)<br>Carro Preto, Chassi 8AF..., 10L no tanque."]

        Classe -->|Instanciação com new| Objeto1
        Classe -->|Instanciação com new| Objeto2
    end
```

Na Ciência da Computação:
* **[[Glossário de conceitos#3 Classe Class|Classe (Class)]]:** É o molde, modelo ou especificação abstrata que define um novo [[Glossário de conceitos#1 Tipo Type|Tipo de Dado]] no programa. A classe não ocupa espaço de processamento de dados em si; ela apenas descreve a estrutura $\rightarrow$ [[00. Sintaxe Multilinguagem/05. Abstração e encapsulamento com classes (TADs)|Sintaxe Comparada de Classes]].
* **[[Glossário de conceitos#Objeto Instance|Objeto (Object / Instance)]]:** É a entidade viva e concreta alocada na memória RAM a partir daquele molde via instanciação (`new`). Você pode criar infinitos objetos a partir de uma única classe, e cada um terá sua própria identidade e valores independentes.
* **[[Glossário de conceitos#4 Atributo Attribute Field|Atributo (Field / Data Member)]]:** São as variáveis internas que representam os dados estruturais do objeto e armazenam o seu **[[Glossário de conceitos#5 Estado State|Estado (State)]]** (a fotografia dos valores em um dado instante).
* **[[Glossário de conceitos#6 Método Method|Método (Method / Member Function)]]:** São as sub-rotinas ([[02. Funções e procedimentos|Funções e Procedimentos]]) que definem o **comportamento** do objeto, sendo o canal legítimo para consultar e modificar o estado encapsulado.
* **[[Glossário de conceitos#8 Declaração em Computação Declaration Declarar|Declaração (Declaration)]]:** O ato técnico de apresentar os nomes e tipos dos membros ao compilador (`double _saldo;`), diferenciando a reserva de identidade da sua inicialização e alocação em memória.
* **Sustentação da [[06. Fatores internos de qualidade de software|Qualidade de Código]]:** A separação estrita entre Atributos privados e Métodos públicos garante alta coesão, baixo acoplamento e manutenibilidade contínua.

---

## 2. Anatomia de uma classe: separando estado e comportamento

```mermaid
flowchart LR
    subgraph ClasseContainer ["Estrutura da Classe: ContaBancaria"]
        direction TB
        subgraph Estado ["Atributos (Estado Interno - Privado)"]
            A1["- _numeroConta : int"]
            A2["- _titular : string"]
            A3["- _saldo : double"]
        end

        subgraph Comportamento ["Métodos (Comportamento - Interface Pública)"]
            M1["+ Depositar(valor : double) : void"]
            M2["+ Sacar(valor : double) : bool"]
            M3["+ ObterSaldo() : double"]
        end

        Estado ~~~ Comportamento
    end
```

---

## 3. Definição prática de atributos e métodos em C#

```csharp
using System;

namespace BancoModular 
{
    // A Classe: Molde estrutural
    public class ContaCorrente 
    {
        // 1. Atributos (Dados privados encapsulados)
        private string _numero;
        private string _titular;
        private double _saldo;

        // 2. Construtor: Inicializa os atributos do objeto recém-criado
        public ContaCorrente(string numero, string titular, double saldoInicial) 
        {
            _numero = numero;
            _titular = titular;
            _saldo = saldoInicial >= 0 ? saldoInicial : 0;
        }

        // 3. Método de Procedimento (Modifica o estado sem retornar valor)
        public void Depositar(double valor) 
        {
            if (valor > 0) 
            {
                _saldo += valor;
            }
        }

        // 4. Método com Retorno Booleano (Protege regras de negócio)
        public bool Sacar(double valor) 
        {
            if (valor > 0 && _saldo >= valor) 
            {
                _saldo -= valor;
                return true; // Saque autorizado
            }
            return false; // Saldo insuficiente
        }

        // 5. Método de Função (Retorna dados com segurança)
        public double ObterSaldo() 
        {
            return _saldo;
        }
    }

    class Program 
    {
        static void Main() 
        {
            // Instanciação: Criando objetos na memória
            ContaCorrente contaDoLeo = new ContaCorrente("1001-X", "Leo Ruas", 500.0);
            ContaCorrente contaDaAna = new ContaCorrente("2002-Y", "Ana Maria", 1200.0);

            // Invocando métodos (Comportamento)
            contaDoLeo.Depositar(250.0);
            bool sucesso = contaDoLeo.Sacar(100.0);

            Console.WriteLine($"Saldo Leo: R$ {contaDoLeo.ObterSaldo():F2}"); // R$ 650.00
            Console.WriteLine($"Saldo Ana: R$ {contaDaAna.ObterSaldo():F2}"); // R$ 1200.00 (Intacto)
        }
    }
}
```

---

## 4. Diferenças conceituais: atributos vs. variáveis locais

Muitos estudantes confundem atributos com variáveis normais. A tabela abaixo resume as distinções fundamentais:

| Característica | Atributo (*Field*) | Variável Local (*Local Variable*) |
| :--- | :--- | :--- |
| **Onde é declarado?** | No corpo da classe, fora de qualquer método. | Dentro do corpo de um método ou bloco `{ }`. |
| **Tempo de vida (*Lifecycle*)** | Vive enquanto o **objeto** existir na memória RAM. | Morre assim que o método termina de executar. |
| **Escopo de visibilidade** | Acessível por todos os métodos daquela classe. | Acessível apenas dentro do bloco onde foi criada. |
| **Modificadores de acesso** | Suporta `private`, `public`, `protected`. | Não aceita modificadores de visibilidade. |
| **Inicialização padrão** | Inicializado automaticamente (0, `null`, `false`). | Obrigatório inicializar manualmente antes de usar. |

---

## 5. Boas práticas na definição de atributos e métodos

```mermaid
flowchart TD
    subgraph BoasPraticas ["Diretrizes de Qualidade Interna"]
        direction TB
        D1["1. Atributos SEMPRE Privados (private)<br>Nunca exponha variáveis diretamente para fora da classe."]
        D2["2. Métodos Coesos e Curtos<br>Cada método deve realizar apenas uma tarefa bem definida."]
        D3["3. Parâmetros Validados na Entrada<br>Métodos devem validar regras antes de modificar atributos."]
        D4["4. Ocultamento de Implementação<br>O usuário da classe só conhece a assinatura do método."]

        D1 --> D2
        D2 --> D3
        D3 --> D4
    end
```

---

## Referências bibliográficas

### Bibliografia básica
* MEYER, Bertrand. **Object-Oriented Software Construction**. 2. ed. Prentice Hall, 1997.
* SOMMERVILLE, Ian. **Engenharia de Software**. 10. ed. São Paulo: Pearson, 2019.
* MARTIN, Robert C. **Código Limpo: Habilidades Práticas do Agile Software**. Rio de Janeiro: Alta Books, 2011.

### Bibliografia complementar
* MICROSOFT. **Convenções de codificação em C#**. Guia do Desenvolvedor .NET, 2023. Disponível em: [https://docs.microsoft.com/pt-br/dotnet/csharp/fundamentals/coding-style/coding-conventions](https://docs.microsoft.com/pt-br/dotnet/csharp/fundamentals/coding-style/coding-conventions).
* SEBESTA, Robert W. **Conceitos de Linguagens de Programação**. 11. ed. Porto Alegre: Bookman, 2018.
* TENENBAUM, Aaron M.; LANGSAM, Yedidyah; AUGENSTEIN, Moshe J. **Estruturas de Dados Usando C**. São Paulo: Makron Books, 1995.

---

## Artigos relacionados e navegação

* **Artigo anterior:** [[06. Fatores internos de qualidade de software]]
* **Resumo da disciplina:** [[00. Programação modular - Resumo]]
* **Glossário da disciplina:** [[Glossário de conceitos]]
* **Guia de Sintaxe Multilinguagem:** [[00. Sintaxe Multilinguagem/05. Abstração e encapsulamento com classes (TADs)]]
* **Guia de Sintaxe (Modificadores e static):** [[00. Sintaxe Multilinguagem/06. Modificadores de acesso, escopo e a palavra-chave static]]
* **Guia de Sintaxe (Getters e Setters):** [[00. Sintaxe Multilinguagem/07. Getters, setters e propriedades (acesso e modificação de estado)]]
* **Índice geral do vault:** [[index.md|Página Inicial do Vault]]
