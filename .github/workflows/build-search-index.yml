name: Atualizar índice de busca

on:
  push:
    branches:
      - main
      - search-v3
    paths:
      - "**/*.md"
      - "index.html"
      - "script.js"
      - "search-runtime.js"
      - "search-runtime.css"
      - "scripts/build-search-index.mjs"
      - "scripts/apply-search-v3-patch.mjs"
      - ".github/workflows/build-search-index.yml"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: search-index-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Baixar repositório
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Aplicar migração search v3
        if: ${{ hashFiles('scripts/apply-search-v3-patch.mjs') != '' }}
        run: node scripts/apply-search-v3-patch.mjs

      - name: Validar JavaScript
        run: |
          node --check search-runtime.js
          node --check script.js
          node --check scripts/build-search-index.mjs

      - name: Gerar índice
        run: node scripts/build-search-index.mjs

      - name: Salvar arquivos atualizados
        shell: bash
        run: |
          git add script.js index.html search-index.json
          if git diff --cached --quiet; then
            echo "Busca já está atualizada."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "chore: aplica search v3 [skip ci]"
          git push
