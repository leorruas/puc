---
title: "05. Abstração e encapsulamento com classes (TADs)"
disciplina: "00. Geral"
modulo: "Sintaxe Multilinguagem"
tags:
  - tads
  - classes
  - objetos
  - encapsulamento
  - csharp
  - java
  - python
relacionados:
  - "[[00. Geral/Guia multilinguagem de sintaxe e praticas - Índice]]"
---

# 05. Abstração e encapsulamento com classes (TADs)

> **Contexto:** Estudo prático da criação de classes, proteção de estado interno (atributos privados) e exposição de métodos em C#, Java e Python.

---

## 1. Como funciona

Unifica estado (atributos privados) e comportamento (métodos públicos) em uma classe. Garante que os dados internos não sejam corrompidos por acesso direto não autorizado.

---

## 2. Comparativo multilinguagem

==== C#
```csharp
public class ContaBancaria {
    private double _saldo;

    public ContaBancaria(double saldoInicial) {
        _saldo = saldoInicial;
    }

    public void Depositar(double quantia) {
        if (quantia > 0) _saldo += quantia;
    }

    public double GetSaldo() => _saldo;
}
```

==== Java
```java
public class ContaBancaria {
    private double saldo;

    public ContaBancaria(double saldoInicial) {
        this.saldo = saldoInicial;
    }

    public void depositar(double quantia) {
        if (quantia > 0) this.saldo += quantia;
    }

    public double getSaldo() {
        return this.saldo;
    }
}
```

==== Python
```python
class ContaBancaria:
    def __init__(self, saldo_inicial):
        self._saldo = saldo_inicial # Prefixo _ indica atributo privado por convenção

    def depositar(self, quantia):
        if quantia > 0:
            self._saldo += quantia

    def get_saldo(self):
        return self._saldo
```

---

## Navegação

* **Tópico anterior:** [[00. Geral/Sintaxe Multilinguagem/04. Sub-rotinas (funções e procedimentos)|04. Sub-rotinas (Funções e Procedimentos)]]
* **Voltar ao Índice:** [[00. Geral/Guia multilinguagem de sintaxe e praticas - Índice]]
