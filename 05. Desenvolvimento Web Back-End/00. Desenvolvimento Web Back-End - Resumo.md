# Desenvolvimento Web Back-End

## Visão geral

A disciplina começou pelas decisões estruturais que sustentam aplicações Web. Até agora, o estudo cobre arquitetura de software, cliente-servidor, requisições e respostas HTTP, atributos de qualidade, estilos arquiteturais, modelos de navegação e renderização, linguagens de programação back-end e a introdução a frameworks Web, MVC e mapeamento objeto-relacional com Entity Framework Core, além de Model, validação, injeção de dependência e persistência no ASP.NET Core MVC.

O conteúdo se conecta diretamente a conceitos já estudados em [[01. Programacao Modular/00. Programação modular - Resumo|Programação Modular]], especialmente modularidade, acoplamento, manutenibilidade e fatores de qualidade.

## Mapa de conteúdo

- [x] [[01. Arquitetura de software e cliente-servidor|01. Arquitetura de software e cliente-servidor]]
  - arquitetura de software e descrição arquitetural;
  - visões de arquitetura;
  - cliente-servidor;
  - client-side e server-side;
  - requisições e respostas HTTP;
  - conteúdo estático e dinâmico;
  - características operacionais, estruturais e transversais;
  - trade-offs arquiteturais.

- [x] [[02. Estilos arquiteturais - camadas, monolítico e cliente-servidor|02. Estilos arquiteturais: camadas, monolítico e cliente-servidor]]
  - arquitetura em camadas;
  - distinção entre *layers* e *tiers*;
  - separação lógica e separação física;
  - escalabilidade em cliente-servidor;
  - arquitetura monolítica;
  - simplicidade inicial de teste e *deploy*;
  - diferença entre monolítico e organização em camadas;
  - interpretação do gabarito da PUC sobre esses estilos.

- [x] [[03. Tipos de aplicações Web e estratégias de renderização|03. Tipos de aplicações Web e estratégias de renderização]]
  - IP, portas, TCP e HTTP;
  - DOM e renderização;
  - templates e controllers no servidor;
  - MPA e SPA;
  - AJAX e Fetch;
  - CSR, SSR e SSG;
  - hidratação;
  - SEO e renderização JavaScript;
  - APIs como separação entre interface e backend;
  - distinção entre topologia e tier.

- [x] [[04. Linguagens de programação back-end e PHP|04. Linguagens de programação back-end e PHP]]
  - PHP como foco principal e execução server-side;
  - fluxo requisição → servidor Web → aplicação PHP → banco → resposta;
  - GET, POST, `$_GET` e `$_POST`;
  - Apache, runtime PHP, MySQL e pilha WAMP;
  - linguagem de script versus linguagem de programação;
  - compilação, interpretação e modelos híbridos;
  - tipagem dinâmica no PHP e contraste com tipagem estática;
  - desempenho e portabilidade como eixos independentes da tipagem;
  - templates Web e separação de responsabilidades;
  - comparação final entre PHP e outros ecossistemas back-end.

- [x] [[05. Frameworks Web, MVC e mapeamento objeto-relacional|05. Frameworks Web, MVC e mapeamento objeto-relacional]]
  - framework Web como infraestrutura reutilizável e diferença para biblioteca;
  - abordagens server-centric/browser-centric e frameworks horizontais/verticais;
  - relação conceitual com frameworks de negócios e Service Design;
  - MVC como padrão de organização de responsabilidades;
  - Razor, roteamento e scaffolding;
  - ORM como ponte entre objetos e modelo relacional;
  - impedância objeto-relacional e permanência do SQL sob a abstração;
  - Entity Framework Core;
  - relacionamento 1:N, `ICollection<T>`, `DbContext` e `DbSet<T>`;
  - configuração do contexto e migrations;
  - exemplo completo de persistência com gênero e filme;
  - tutorial de configuração do EF Core no Visual Studio Code no macOS, com C# Dev Kit, .NET SDK, `dotnet-ef`, SQLite, migrations e alternativa com SQL Server/MSSQL.


- [x] [[06. Model, validação e persistência no ASP.NET Core MVC|06. Model, validação e persistência no ASP.NET Core MVC]]
  - integração entre Model, EF Core, injeção de dependência e Controller no ASP.NET Core MVC;
  - distinção entre Model, entidade persistente e DTO;
  - distinção entre classe, propriedade, tipo e Data Annotation;
  - Data Annotations para metadados e validação, incluindo argumentos posicionais e nomeados;
  - distinção entre `DataType.EmailAddress` e `EmailAddress`;
  - `ApplicationDbContext` como especialização de `DbContext` e leitura de `DbContextOptions<TContext>`;
  - ASP.NET Core Identity e uso opcional de `IdentityDbContext`;
  - injeção por construtor e registro do contexto com `AddDbContext`;
  - provider, connection string e `appsettings.json`;
  - uso de `FindAsync` pelo Controller e fluxo até a View;
  - posição das migrations no fluxo sem repetir a definição aprofundada do artigo 05;
  - exemplo integrado com `Pessoa.cs`, `ApplicationDbContext.cs`, `Program.cs`, Controller e configuração;
  - diferença estrutural entre `Startup.cs` e `Program.cs`;
  - referência ao tutorial de ambiente macOS do artigo 05.

- [x] [[05.1. Bônus - Razor na prática no ASP.NET Core MVC|05.1. Bônus - Razor na prática no ASP.NET Core MVC]]
  - papel de Razor em projetos ASP.NET Core MVC com Views `.cshtml`;
  - transição entre HTML e C# com `@`;
  - diferença entre `@model` e `Model`;
  - condicionais e `foreach` para lógica de apresentação;
  - Tag Helpers como `asp-for`, `asp-action` e `asp-validation-for`;
  - integração entre formulários, model binding, validação e Controller;
  - layouts com `@RenderBody()` e `_ViewStart.cshtml`;
  - partial views para reutilização de interface;
  - fronteira entre lógica de apresentação e regras de negócio;
  - distinção entre Razor, Razor Pages e Blazor.

Os próximos conteúdos serão adicionados conforme forem estudados, sem antecipar a estrutura da disciplina a partir de tecnologias citadas isoladamente.

## Pontos centrais até agora

Arquitetura não é apenas uma escolha de framework ou linguagem. Ela representa decisões sobre organização, responsabilidades, relações, implantação e princípios de evolução do sistema.

Em aplicações Web, a separação cliente-servidor ajuda a localizar responsabilidades: o cliente inicia requisições e o servidor processa essas requisições e produz respostas. A comunicação ocorre por protocolos e portas, sendo 80 a porta padrão do HTTP e 443 a do HTTPS.

Em arquitetura em camadas, *layers* representam separações lógicas de responsabilidade, enquanto *tiers* representam separações físicas ou de implantação. Uma aplicação pode possuir várias layers executando em um único tier.

Uma arquitetura monolítica agrupa a aplicação em uma unidade principal de execução e implantação. Isso pode simplificar testes integrados e *deploy* inicialmente, embora essa vantagem dependa do tamanho e do contexto do sistema. Monolítico e camadas não são necessariamente opostos: um monólito pode ser organizado internamente em camadas.

MPA e SPA descrevem principalmente como a navegação e a atualização da interface são organizadas. CSR, SSR e SSG respondem a outra pergunta: onde ou quando a interface é renderizada. Por isso, aplicações modernas podem combinar essas estratégias em vez de pertencer a uma única categoria rígida.

No lado do servidor, PHP é usado como eixo principal para entender execução server-side, formulários, runtime, servidor Web, banco de dados, WAMP e templates. A partir desse caso concreto, compilação/interpretação, tipagem, portabilidade e diferenças entre ecossistemas são comparadas sem tirar o PHP do centro da explicação.

Frameworks Web reutilizam arquitetura e infraestrutura para problemas recorrentes como roteamento, validação, autenticação, views e persistência. O conteúdo avançou para MVC e para o mapeamento objeto-relacional, em que o Entity Framework Core traduz entre classes C# e estruturas relacionais. O estudo agora também conecta o Model e suas Data Annotations ao `ApplicationDbContext`, à injeção de dependência em controllers, às connection strings e às migrations.

Características arquiteturais expressam qualidades que o sistema precisa apresentar, como desempenho, escalabilidade, disponibilidade, segurança, manutenibilidade e portabilidade. Como essas características podem competir entre si, decisões de arquitetura envolvem trade-offs.

## Glossário

Os termos técnicos acumulados pela disciplina estão consolidados em [[05. Desenvolvimento Web Back-End/Glossário de conceitos|Glossário de conceitos]]. O glossário diferencia conceitos próximos, registra conexões com outras matérias e, quando necessário, separa a simplificação utilizada pela PUC da ressalva técnica mais ampla.

## Guia de estudos com LLM

Para estudar esta matéria usando Inteligência Artificial, consulte os [[05. Desenvolvimento Web Back-End/Prompts de Estudo (LLM)|Prompts de Estudo (LLM)]].
